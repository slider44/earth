import { PositionsType } from './dock-monitor/reducer';
export interface DevConfig {
    position?: PositionsType;
    visible?: boolean;
    size?: number;
}
export declare let config: DevConfig;
export declare const devtoolsConfig: (settings: DevConfig) => any[];
